#!/usr/bin/env python3

words = ['timber',
         'operate',
         'restore',
         'mention',
         'naval',
         'mystery']

print(len([w for w in words if len(w) > 6]))

# done
# done